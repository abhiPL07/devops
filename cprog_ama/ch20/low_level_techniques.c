#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

static typedef unsigned char BYTE;

int runLlt(void) {
    // typedef unsigned char BYTE;
    // BYTE *p;
    // p = (BYTE *) 0x1000;
    // printf("At address %hhn:\t%hx\n", p, *p);
    return 0;
}

int viewMemLoc(unsigned int addr, int n) {
    // int i, n;
    // BYTE *ptr;
    // printf("Address of viewMemLoc: %x\n", (unsigned int) viewMemLoc);
    // printf("Address of addr: %x\n", (unsigned int) &addr);
    return 0;
}